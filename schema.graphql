type Collected @entity(immutable: true) {
  id: ID!
  collector: String!
  allocatee: String!
  amount: BigInt!
  collected: BigInt!
  expiredAt: BigInt!
}

type Redeemed @entity(immutable: true) {
  id: ID!
  user: String!
  amount: BigInt!
  totalCost: BigInt!
}
